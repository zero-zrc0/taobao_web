spring:
  application:
    name: taobao_system
  datasource:
    url: jdbc:mysql://8.134.99.71:13306/taobao?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: webdb
    password: web123123
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.taobao.pojo
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  type-handlers-package: org.taobao.typehandler

logging:
  level:
    org.taobao.mapper: debug
    org.springframework.jdbc.support.JdbcTransactionManager: debug

# 阿里云OSS
aliyun:
  oss:
    endpoint: https://oss-cn-beijing.aliyuncs.com
    bucketName: taobao-web
    region: cn-beijing

server:
  port: 8085
  servlet:
    context-path: /api