<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.taobao.mapper.UserMapper">

    <!-- 定义结果映射 -->
    <resultMap id="UserResultMap" type="org.taobao.pojo.User">
        <id column="user_id" property="userId"/>
        <result column="account" property="account"/>
        <result column="password" property="password"/>
        <result column="user_type" property="userType"/>
        <result column="status" property="status"/>
        <result column="username" property="username"/>
        <result column="gender" property="gender"/>
        <result column="birthday" property="birthday"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 查询用户列表 -->
    <select id="getUserList" parameterType="org.taobao.dto.UserQueryDTO" resultMap="UserResultMap">
        SELECT user_id, account, password, user_type, status, username, gender, birthday, phone, email, avatar_url, create_time, update_time FROM user
        <!--动态SQL条件拼接，根据传入参数自动生成WHERE子句-->
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="account != null and account != ''">
                AND account LIKE CONCAT('%', #{account}, '%')
            </if>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="userType != null and userType != ''">
                AND user_type = #{userType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="pageNum != null and pageSize != null">
            LIMIT #{pageSize} OFFSET #{offset}
        </if>
    </select>

    <!-- 查询用户总数 -->
    <select id="getUserCount" parameterType="org.taobao.dto.UserQueryDTO" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM user
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="account != null and account != ''">
                AND account LIKE CONCAT('%', #{account}, '%')
            </if>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="userType != null and userType != ''">
                AND user_type = #{userType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 更新用户状态 -->
    <update id="updateStatus">
        UPDATE user SET status = #{status}, update_time = NOW() WHERE user_id = #{userId}
    </update>
</mapper>